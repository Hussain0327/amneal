# golangci-lint v2 config for the go/ module (CI runs the version pinned in
# ci.yml's golangci-lint step). Standard default linter set; generated sqlc
# files are auto-excluded by the default `linters.exclusions.generated: lax`
# (they carry the "Code generated by sqlc. DO NOT EDIT." header).
version: "2"
